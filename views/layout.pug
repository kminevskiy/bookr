include /_mixins

doctype html
html
  head
    title Bookr
    link(href="https://fonts.googleapis.com/css?family=Lato:400,700" rel="stylesheet")
    +stylesheets("reset")
    +stylesheets("style")
    +javascripts("/vendor/all")
    +javascripts("handlebars/templates")
  body
    header
      nav
    main
      div#modal
      div#content
    footer
    block content
  +javascripts("application")
  +javascripts("models/book")
  +javascripts("collections/books", "collections/books_to_read")
  +javascripts("views/new", "views/index", "views/book", "views/menu", "views/edit_modal", "views/error_modal", "views/details_modal", "views/empty_stub", "views/read_index", "views/read_menu", "views/new_toread", "views/book_to_read")
  +javascripts("routers/router")
  script(type="text/javascript").
    app.init()
